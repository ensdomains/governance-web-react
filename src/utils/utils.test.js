describe("formatTokenAmount", () => {
  it.todo("use each to provider lots of test cases");
  it.todo("should be able to handle largest token amount")
});

describe("imageUrl", () => {
  it.todo(
    "should return the correct url if networkName and protocol are supported"
  );
});
