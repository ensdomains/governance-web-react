describe("ENSSummary", () => {
  it.todo("should not allow user to try and claim if they have already claimed")
  it.todo("should display address if delegated to address")
  it.todo(
    "should display ens name with profile pic if delegated to profile pic"
  )
})
