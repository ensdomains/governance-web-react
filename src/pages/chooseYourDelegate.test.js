describe("useGetDelegates", () => {
  it.todo("should not run if we do not have a provider")
  it.todo(
    "should filter out entries where the resolver address does not match the domain resolver address"
  )
  it.todo("should filter out names that do not have the correct fields set")
})

describe("addBalance", () => {
  it.todo("should add allocation and current votes to ranking")
})
